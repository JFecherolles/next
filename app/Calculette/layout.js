

export default function AboutLayout({ children }) {
  return (
    <section>
      {/* Incluez ici les éléments d'interface utilisateur partagés, par exemple un en-tête ou une barre latérale */}
      <nav></nav>
 
      {children}
    </section>
  );
}
